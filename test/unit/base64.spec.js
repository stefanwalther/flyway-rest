/* global describe, it */
describe( 'base64', () => {

  it( 'encodes a single file', () => {
    expect( false ).to.be.true;
  } );

  it( 'encodes a collection of files', () => {
    expect( false ).to.be.true;
  } );

} );
