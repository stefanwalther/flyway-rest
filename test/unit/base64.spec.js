/* global describe, it */
describe( 'base64', () => {

  xit( 'encodes a single file', () => {
    expect( false ).to.be.true;
  } );

  xit( 'encodes a collection of files', () => {
    expect( false ).to.be.true;
  } );

} );
