sudo: required
language: node_js

services:
  - docker

cache:
  directories:
    - node_modules

env:
  DOCKER_COMPOSE_VERSION: 1.8.0

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

before_script:
  - cd ./test/integration/
  - docker-compose up -p ci --build d
  - sleep 3

script:
  - docker version
  - docker-compose version

after_script:
  - docker-compose kill

#script: chmod a+x ./test/integration/test.sh && ./test/integration/test.sh
